clear all;
close all;
clc;
plot_p = 0;
[N,T,Z,F,Hfull,mX0,PX0,Qw,Rv,X] = simulationDonnees(plot_p);
Part = 2;
%w1 = [];
%x1 = [];
X = zeros(6,Part,N);
W = zeros(1,Part,N);
%k=0 :
[x,w] = SIS(zeros(6,Part),zeros(1,Part),[],0);
X(:,:,1) = x;
W(:,:,1) = w;

%k>0 :
for k = 1:N-1
    [x,w] = SIS(X(:,:,k),W(:,:,k),Z(:,k),k); %x : (6,Part) w : (1,Part)
    X(:,:,k+1)=x;
    W(:,:,k+1)=w;
end
